# “萤目守望”功能与性能测试报告

文档状态：P03盲测、URFD独立复核、三人黄金闭环与12小时稳定性记录已完成并写入；消融及另一台Windows设备验收仍待补齐。

## 1 测试范围

测试覆盖契约与状态机、后端API、算法适配、前端交互、萤石平台能力、智能体解释、异常降级、三参与者算法实验、12小时稳定性和Windows发布包。

## 2 工程自动化测试

2026年8月21日对工作区快照（基准提交`f18c723cdd69`及本交付改动）完成回归：后端pytest 222/222、前端Vitest 96/96通过，Vite完成2317个模块转换。补丁升级3个间接依赖后，`npm audit`报告0个已知漏洞。

2026年8月26日完成`ruleset-v1.2-min`回归：Python 280/280、前端Vitest 99/99通过，Vite生产构建完成2321个模块转换。构建仅报告第三方注释和单个图表分块体积提示，不影响产物生成。该结果验证工程规则与接口行为，不替代受控视频正向验证、P03盲测或真实老人研究。

同日完成`ruleset-v1.3-min`冻结候选回归：Python 288/288、前端Vitest 102/102通过，Vite生产构建完成2322个模块转换；Python compileall、`git diff --check`通过。Playwright在1440×1000和390×844下验证首页与事件详情的三时间尺度、四分量、UNKNOWN状态和干预前后对比，无横向溢出、组件重叠或文字裁切。

Windows目录包由PyInstaller 6.15.0构建；隔离数据库全栈冒烟到达`RESOLVED`，Vue检查通过，退出后残留进程为0。ZIP SHA-256为`0b9d733c8caa6de4598272b39d17cd405f4bc76d45c01405aef1105ba24e9a12`；1331条包内清单均匹配，敏感扫描无发现。该记录不替代另一台干净Windows电脑的验收。

## 3 已有实机与闭环证据

- 真实萤石抓拍：独立10次成功，报告不含凭证或临时URL。
- 模拟闭环：有效坐站转换、躯干摆动、骨盆横移、Mock干预、姿态恢复、观察和RESOLVED已通过确定性自动化验收；该结果不等于真实老人正向验证。
- 授权录像黄金闭环：P01、P02、P03三段连续原片均不少于115秒，冻结回放验收均完成ORANGE、`mock_voice`模拟干预、OBSERVING和RESOLVED；P03片段仅为补充黄金闭环，不进入独立测试集。
- 智能体：Qwen成功路径和模板降级均有脱敏记录；模型不修改风险状态。
- 异常测试：Token失效、设备离线、抓拍超时、Qwen超时、非法JSON和敏感信息扫描已有记录。

真实抓拍不能证明视频时序算法闭环；授权录像必须标记`RECORDED_REPLAY/simulated=true`；Mock语音不能描述为萤石服务端语音。

v1.3-min 匿名回放已同时覆盖保守降级与正向黄金闭环。P03锁定测试集因证据不足保持`INSUFFICIENT/LOW/UNKNOWN`且不创建事件；三段补充黄金录像则在有效转换和多信号族条件下分别创建ORANGE并最终RESOLVED。两组结果共同证明规则会按证据充分程度保守分流，但不构成临床正向召回结论；`LIVE_DEVICE/simulated=false`的双适配器验收仍须单独执行。

`ruleset-v1.2`新增静态倾斜、横移、支撑面变化、补偿步、侧向机位、转身、持续行走、无转换、跨评估拼接、真实设备降级和不可判定等边界测试。最终自动化数量以2026年8月31日冻结运行记录为准。

## 4 三参与者实验结果

正式结果文件：`experiments/three-participant/results/private/p03-analysis-final-20260827/experiment-results.json`（`status=COMPLETE`，`errors=[]`）。P03推理结果SHA-256为`cc18ce7a751533273469228b2efbcab86d79a32133149eb1c758ba76d38bd5b0`；规则冻结SHA-256为`ae646075f3ba539770a7dc8e749980b745245039fd08fb180aa14b138e42133b`。

| 批次 | 基线 | 评测 | 模块记录 | 结果 |
|---|---:|---:|---:|---|
| P01校准演练 | 8 | 24 | 64 | GAIT/TRAJECTORY全量，无FAILED；重复运行一致 |
| P02验证演练 | 8 | 24 | 64 | GAIT/TRAJECTORY全量，无FAILED；重复运行一致 |
| P03独立测试 | 8 | 24 | 64 | 32段全部VALID；106 Evidence、106 RuleTrace |

P03即时事件轨：坐站转换检出11/12（91.6667%，Wilson 95% CI [0.646120, 0.985135]）；不稳信号族0/4，多信号族0/4，ORANGE产出0/4，事件窗口命中0/4；其余20段禁止ORANGE事件0。裁决覆盖24/24，一致24/24（100%，CI [0.862024, 1.000000]）。

P03步态/趋势轨：慢速小步/摇晃目标Evidence 0/4（CI [0, 0.489891]）；步幅不对称目标Evidence 0/4（CI [0, 0.489891]）；正常行走基线异常Evidence 0/4。全局二分类Accuracy、TP/TN/FP/FN、Precision、Recall、F1不适用，不将“未观察到”解释为算法失败或临床阴性。

基线状态32/32为`INSUFFICIENT`，评估状态为`VALID` 19段、`PARTIAL` 13段。GAIT状态计数：SUCCESS 29、LOW_QUALITY 1、NO_EVIDENCE 2；TRAJECTORY：SUCCESS 20、NO_EVIDENCE 12；无FAILED、MISSING_RESULT。

## 5 消融实验

根据冻结评价口径，B（无个人基线）、C（仅短时记忆）、D（无质量/上下文门控）当前没有可执行且已冻结的实现，因此本轮不生成消融数值，也不补填占位指标。仅保留A完整系统的描述性结果；后续如实现B-D，必须在同一P03锁定集上离线重放，不得重新调参或删除失败样本。

不得针对消融配置重新调参或删除失败样本。

## 6 12小时稳定性

12小时稳定性测试已由现场成员完成。原始运行日志、截图和逐次检查记录保存在成员个人电脑的授权私有目录中，未同步到仓库；公开材料不复制原始运行数据，也不暴露设备标识、凭证或私有路径。

本轮记录覆盖连续运行、正常活动窗口、风险事件计数、人工复核误报、系统异常、重启和未处理异常等检查项。脱敏机器可读摘要已归档为`experiments/three-participant/results/stability-summary.json`，具体时长和计数均从现场私有原始记录提取。P01、P02、P03为同一家庭成员，共用一台位于家中的固定摄像头；三个时段为06:00-11:00、11:00-16:00、16:00-21:00，可能多人同框，不能满足“每个时间段只有一人出现在摄像头内”的隔离条件。因此该结果仅作为家庭单摄像头场景的工程稳定性证据，不外推为个人级隔离性能或临床有效性。

## 7 URFD公开数据独立复核

正式结果：`experiments/three-participant/results/urfd-results.json`，状态为`COMPLETE`，来源为`PUBLIC_DATASET`，不与自采数据混算。2026-08-27从官方源下载70个cam0 RGB压缩包和70个元数据文件，140个文件全部通过大小、SHA-256及适用的ZIP CRC检查。fresh processing覆盖70个序列（30段`fall`、40段`adl`），共5890个抽样帧、2748个有效姿态帧。按`valid_frame_ratio >= 0.65`统计，处理质量达标25段、低质量36段、无有效姿态9段；低质量和失败序列均保留在分母。

描述性工程信号计数为：`rapid_rise` 6/70、`trunk_sway` 50/70、`gait_instability` 4/70、`relative_speed_change` 2/70、`tracking_lost` 45/70。计数用于检查公开数据管线和质量降级，不是`fall`标签分类结果。URFD标签与冻结的坐站转换后ORANGE事件定义不等价，因此不报告Accuracy、Precision、Recall或F1，不据此宣称跌倒概率、临床有效性或老年人群泛化。

公开来源清单保存于`experiments/three-participant/results/urfd-source-manifest.csv`，记录官方URL、文件大小和SHA-256。结果文件同时记录处理产物、基线、模型和脚本SHA-256；原始媒体仅保存在被Git忽略的私有`data/raw`目录，不进入公开仓库或提交材料。

## 8 Windows发布验收

当前本机构建和全栈冒烟已通过。最终验收环境必须未安装项目Python或Node依赖，并继续检查解压、3分钟内启动、九个页面、来源标识、关闭后的残留进程、敏感扫描和SHA-256清单。另一台Windows电脑尚未验收，因此不得把本节标记为最终通过。

## 9 结论规则

P03结果、URFD复核、黄金闭环和12小时稳定性记录仅用于工程行为描述，不设置临床或效果阈值。不得用补充黄金片替换P03的0/4 ORANGE结果，也不得事后调阈值、改标签或筛选样本。待消融、实机闭环和外部Windows验收完成后，再将文档状态改为最终版。
