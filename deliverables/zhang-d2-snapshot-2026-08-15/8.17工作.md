附件中的部分状态已经过时：真实 Qwen、RiskEvent 解释持久化、MP4 算法链路和 Mock 干预恢复闭环目前都已完成。你现在只负责张同学工作，应退出后端开发，进入平台能力确认、智能体验收和材料整理阶段。

**你当前负责的工作**

1. 萤石能力矩阵最终确认。
2. 智能体真实模型成功与模板降级验收。
3. 向冷同学交付后端联调要求，但不修改 Worker、数据库和状态机。
4. 向陈同学提供智能体字段、状态和展示口径。
5. 整理脱敏证据、演示流程和答辩材料。

**第一步：把后端工作正式交给冷同学**

将 [PR #51](https://github.com/VIVIAN123-GIF/-YingMu-ShouWang/pull/51) 发给冷同学，请她负责：

- 审查并合并到 `main`；
- 启动 API、Alarm Worker 和 Agent Worker；
- 提供联调用后端地址；
- 提供新的 `event_id`；
- 保证 Explanation 持久化；
- 保证干预与恢复状态机运行；
- 处理后续后端错误。

你不再修改：

```text
AlgorithmTaskService
Alarm Worker
数据库表
RiskEvent 状态机
录像入库脚本
算法超时和重试
```

你只验收其输出是否满足智能体合同。

**第二步：更新能力矩阵**

最终建议记录：

| 能力 | 当前状态 |
|---|---|
| Token 获取 | 已验证 |
| 设备状态 | 已验证 |
| WebHook 真实回调 | 已验证 |
| 真实抓拍 | 已验证，10次中6次成功，失败主要为超时 |
| WebHook 自动抓拍 | 已验证 |
| Asset 私有入库 | 已验证 |
| 临时播放地址 | 部分验证或未稳定验证 |
| 萤石 `qwen3.6-flash` | 已验证 |
| RiskEvent 智能体解释 | 已验证 |
| 模板解释降级 | 已实现并通过测试 |
| Mock 语音/文字提醒 | 已实现 |
| 萤石服务端语音 | 未验证 |
| 授权 MP4 风险闭环 | 已验证，`RECORDED_REPLAY/simulated=true` |
| 真实设备时序闭环 | 尚未完整验证 |

演示路线正式冻结为：

```text
真实萤石 WebHook/抓拍
+ 授权 C6c 录像回放算法闭环
+ 萤石 Qwen 智能体解释
+ Mock 语音干预
```

不要继续反复测试播放地址和服务端语音。

**第三步：完成智能体最终验收**

让冷同学提供一个真实联调事件，你只检查：

```text
GET /api/v1/events/{event_id}/explanation
```

成功路径必须满足：

```json
{
  "status": "SUCCESS",
  "generated_by": "qwen3.6-flash",
  "fallback_used": false
}
```

降级路径必须满足：

```json
{
  "status": "FALLBACK",
  "generated_by": "template-fallback-v1",
  "fallback_used": true
}
```

同时确认：

- Explanation 时间带 `+08:00`；
- 模型只读取 RiskEvent、Evidence 和基线摘要；
- 不上传图片、视频或音频；
- 模型不修改风险等级、分数、状态和规则 ID；
- 页面查询不会重复调用模型；
- 模型失败不阻断 RiskEvent 和干预链路。

真实 Qwen 不需要反复调用。保留一条成功记录即可，降级使用错误模型配置或自动化测试证明。

**第四步：向陈同学交付展示合同**

你给陈同学：

```text
summary
reasoning_points
recommended_action_text
capability_notice
generated_by
fallback_used
created_at
completed_at
```

要求页面明确区分：

```text
风险等级：风险引擎结果
Evidence：算法结果
智能体解释：Qwen或模板结果
能力限制：服务端语音未验证
来源标签：LIVE_DEVICE / RECORDED_REPLAY / MOCK
```

你不负责修改前端。陈同学必须给你：

- Qwen `SUCCESS` 页面截图；
- 模板 `FALLBACK` 页面截图；
- `RECORDED_REPLAY` 来源截图；
- Explanation 北京时间截图；
- 风险、解释、干预三个区域分开展示的截图；
- `npm test` 和 `npm run build` 结果；
- 一段前端闭环录屏。

**第五步：整理张同学最终交付物**

你最终应交付：

1. 萤石开放平台能力矩阵。
2. 真实抓拍测试报告和现场签字记录。
3. WebHook 与自动抓拍验证记录。
4. Qwen 成功调用脱敏报告。
5. 模板降级验证记录。
6. 智能体输入输出接口说明。
7. Mock 语音和文字提醒能力说明。
8. 与陈同学联调的页面截图和录屏。
9. 一份真实设备主演示流程。
10. 一份离线回放备用演示流程。

当前 MP4 闭环证据已保存于 [closed-loop-validation.json](C:/Users/VIVIAN/OneDrive/Desktop/荧目守望/deliverables/lyt-mp4-worker-e2e/closed-loop-validation.json)。

你的下一项实际工作是：把 PR #51 和后端要求交给冷同学，同时更新最终能力矩阵；等冷同学提供稳定 API 和事件编号后，再与陈同学完成前端验收。