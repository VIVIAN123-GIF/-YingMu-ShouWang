你尚未完成的工作主要分为六类。重点已经从开发转为联调、异常验证、三次复现和答辩材料。

**一、PR交接与后端确认**

[PR #51](https://github.com/VIVIAN123-GIF/-YingMu-ShouWang/pull/51) 仍需冷同学完成后端责任确认。

你需要做的是：

- 把PR和闭环报告发给冷同学；
- 请冷同学审查并合并；
- 让冷同学提供稳定的后端启动方式、API地址和新事件编号；
- 确认API、Alarm Worker、Agent Worker均能正常运行；
- 确认Explanation和InterventionResult能够持久化。

你不负责继续修改其中的Worker、数据库和状态机代码。

完成标志：

```text
PR已合并
冷同学确认接管
最新main测试通过
联调API地址和event_id已提供
```

**二、D10语言与行为轨迹扩展场景**

跌倒主线已经完成，但团队D10要求的两个扩展场景尚未正式验收：

1. 一个语言回应场景：
   - “我没事”；
   - 或“需要帮助”。

2. 一个行为轨迹场景：
   - 异常徘徊；
   - 危险区域停留；
   - 活动范围明显变化；
   - 或路线偏离。

常同学负责算法，冷同学负责接入。你只负责：

- 获取对应RiskEvent或Evidence；
- 让智能体生成解释；
- 检查解释没有医疗诊断或诈骗定性；
- 确认模拟音频、录像轨迹明确标记来源；
- 保存一条语言解释和一条轨迹解释。

完成标志：

```text
一个语言Evidence进入统一时间线
一个轨迹Evidence进入统一时间线
两个场景都能生成智能体解释
```

你目前不能直接完成 D10 验收，还需要常同学提供素材、冷同学补齐两处接线。现有代码状态是：

- 语言适配器已存在，输出 `STABLE/HELP`，后端保存为 `InterventionResult`，不是语言 Evidence。
- 轨迹适配器已存在，可输出 `unusual_pacing` Evidence。
- 当前轨迹 Evidence 不会独立创建 RiskEvent，也不会自动进入智能体解释。
- 当前智能体请求只知道“干预成功”，不知道具体是“我没事”还是“需要帮助”。

因此，正确验收口径应调整为：

```text
语言回应以 InterventionResult 进入统一时间线
轨迹结果以 Evidence 进入统一时间线
二者关联 RiskEvent 后分别生成智能体解释
```

**第一步：让常同学提供两组验收输入**

直接发给常同学：

```text
请提供D10扩展场景联调材料：

1. 一段已授权的“需要帮助”音频，建议WAV格式；
2. 对应LANGUAGE AdapterBatch，预期：
   resident_response_candidate.intent=HELP；
3. 一段异常徘徊录像或轨迹数据；
4. 对应TRAJECTORY AdapterBatch，预期包含：
   evidence_type=unusual_pacing；
5. 所有结果必须标记：
   source_mode=RECORDED_REPLAY或MOCK
   simulated=true；
6. 不保存或输出原始语音转写，不进行医疗诊断或诈骗定性；
7. 请同时提供预期Observation、Evidence、质量分数和触发依据。
```

推荐选择“需要帮助”，因为它不会被理解为风险已经解除。

**第二步：让冷同学完成接线**

直接发给冷同学：

```text
D10语言与轨迹验收需要补齐两处后端接线：

1. 语言场景
- 支持授权WAV作为RECORDED_REPLAY/simulated=true的私有Asset入队；
- LANGUAGE AdapterBatch中的HELP/STABLE写入对应活动RiskEvent的InterventionResult；
- 智能体解释请求必须包含脱敏后的回应语义，例如：
  resident_response_help / resident_response_stable；
- 不传原始转写、音频或媒体路径；
- 写入后生成新的AgentExplanationJob。

2. 轨迹场景
- 将unusual_pacing Evidence关联到同一居民的活动RiskEvent统一时间线；
- 只作为扩展上下文，不修改原有风险等级；
- 更新event evidence_ids/related_domains后生成新的AgentExplanationJob。

验收要求：
语言回应和轨迹Evidence都能在GET /api/v1/events/{event_id}查询；
两者都能通过GET /api/v1/events/{event_id}/explanation获得解释。
```

**第三步：联合运行**

启动三个进程：

```powershell
# 终端1
python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000

# 终端2
python -m backend.worker.alarm_worker

# 终端3
python -m backend.worker.agent_worker
```

先为同一居民准备一个尚未解除的 RiskEvent，再依次投递：

1. “需要帮助”授权音频；
2. 异常徘徊录像或轨迹输入。

分别查询：

```powershell
Invoke-RestMethod "http://127.0.0.1:8000/api/v1/events/$eventId" |
  ConvertTo-Json -Depth 20

Invoke-RestMethod "http://127.0.0.1:8000/api/v1/events/$eventId/explanation" |
  ConvertTo-Json -Depth 20
```

**第四步：你负责验收**

语言场景应看到：

```text
action_type=resident_response
resident_response=help
source_mode=RECORDED_REPLAY或MOCK
simulated=true
Explanation=SUCCESS或FALLBACK
```

轨迹场景应看到：

```text
evidence_type=unusual_pacing
risk_domain=MENTAL
source_mode=RECORDED_REPLAY或MOCK
simulated=true
Evidence已关联RiskEvent
Explanation=SUCCESS或FALLBACK
```

解释允许出现：

```text
检测到求助表达，建议人工确认
观察到反复往返活动模式，建议结合个人基线继续观察
```

禁止出现：

```text
老人患有精神疾病
老人已经痴呆
老人已遭遇诈骗
该人员是诈骗分子
```

**第五步：保存交付材料**

建议保存到：

```text
deliverables/zhang-d10-extension/
├─ language-event-detail.json
├─ language-explanation.json
├─ trajectory-event-detail.json
├─ trajectory-explanation.json
├─ language-page.png
├─ trajectory-page.png
└─ D10扩展场景验收记录.md
```

不要提交原始音频、录像、设备序列号、Token、媒体路径或完整语音转写。完成以上内容，才能正式认定 D10 的语言和行为轨迹扩展场景通过验收。



**三、D11异常场景验证**

你负责的异常测试还需形成正式、脱敏的验收材料。

| 场景 | 预期结果 | 你的交付物 |
|---|---|---|
| Token失效 | 只刷新一次或返回明确错误 | 脱敏错误码和请求耗时 |
| 设备离线 | 明确返回离线，不无限重试 | 状态查询记录 |
| 抓拍超时 | 有限重试后失败 | 现有4次超时记录和原因汇总 |
| Qwen超时 | 自动使用模板解释 | `FALLBACK`响应 |
| Qwen非法JSON | 不显示非法内容，自动降级 | 自动化测试结果 |
| Qwen正常 | 返回真实模型解释 | `SUCCESS`响应 |
| 敏感信息检查 | 不出现凭证和媒体地址 | 脱敏扫描结果 |

Qwen成功路径只需保留一条正式记录，不要反复消耗额度。

降级结果必须满足：

```text
generated_by=template-fallback-v1
fallback_used=true
```

**四、与陈同学完成前端验收**

目前前端代码已经具备接口，但正式联调证据还没整理完成。

你需要给陈同学：

- 后端API地址；
- 新的`event_id`；
- Explanation字段说明；
- `SUCCESS/FALLBACK`预期结果；
- 来源标签要求；
- 萤石服务端语音未验证的能力提示。

陈同学需要交回：

- `INTERVENING`页面截图；
- `RESOLVED`页面截图；
- Qwen成功解释截图；
- 模板降级截图；
- `RECORDED_REPLAY`来源截图；
- 北京时间截图；
- `/intervene`和`/results`脱敏网络请求；
- `npm test`与`npm run build`结果；
- 一段闭环录屏。

还必须验证“我已坐稳”只记录：

```text
resident_response=stable
resolved=false
```

不能由前端直接关闭事件。

**五、D12三次连续闭环复现**

当前只有一次完整MP4闭环通过，还缺两次。

每次必须覆盖：

```text
MP4
→ Observation
→ rapid_rise + trunk_sway
→ RiskEvent
→ 智能体解释
→ Mock干预
→ posture_recovered
→ OBSERVING
→ RESOLVED
```

每次保存：

- Task状态；
- Observation；
- Evidence；
- RiskEvent；
- RuleTrace；
- InterventionResult；
- Explanation；
- 页面截图；
- 脱敏日志；
- 成功或失败结论。

三次不要求全部调用真实Qwen，可以采用：

```text
第1次：Qwen SUCCESS
第2次：Template FALLBACK
第3次：Qwen SUCCESS或已有成功结果复用
```

但三次风险闭环本身必须独立复现。

**六、最终能力矩阵与答辩材料**

能力矩阵还需要按最终结果更新，尤其明确：

- Qwen文本解释已经验证；
- 临时播放地址不稳定；
- 萤石服务端语音未验证；
- Mock语音属于降级；
- MP4闭环是授权录像回放；
- 真实设备时序闭环没有完整验证；
- 真实抓拍不能生成快速起身等时序Evidence。

D12通过后，你还需要参与：

- 录制真实设备主演示；
- 录制完全离线备用演示；
- 编写“黄金半分钟”演示流程；
- 核对PPT中的模型、准确率、数据量和能力表述；
- 准备回答萤石调用、智能体边界、隐私和降级策略。

你当前最优先的三项任务是：

1. 将PR #51交给冷同学并取得稳定联调接口。
2. 与陈同学完成前端SUCCESS/FALLBACK和闭环展示。
3. 再完成两次闭环复现，达到D12硬性检查点。