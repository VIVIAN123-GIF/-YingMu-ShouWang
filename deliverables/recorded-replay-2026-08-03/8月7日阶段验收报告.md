# 8月7日阶段验收报告（2026-08-03）

## 结论

无新增拍摄条件下的软件开发、三段授权 C6c 素材处理、冻结契约入库、Mock 全闭环和前端闭环均已完成。

当前真实素材不能声明黄金闭环完成：黄金片段虽然检测到快速起身、躯干摇晃和超过15秒的稳定姿态，但两条危险证据最高置信度为0.788，未达到“至少一条不低于0.80”；稳定15秒后只剩约32.8秒视频，也未完成60秒观察。因此真实包保持 `PENDING_ASSET / CONFIDENCE_BLOCKED`，没有产生 ORANGE 事件，更不能声明 `RESOLVED`。

个人基线保持 `INSUFFICIENT / 样本不足`。现有三段均为危险或控制素材，禁止进入正常基线。

## 真实素材结果

| 场景 | 自动检测 | HTTP结果 | 阶段结论 |
|---|---|---|---|
| 黄金片段 | 起身0.400秒；摆动22.077°；连续稳定49.466秒；稳定角7.878° | `R-FALL-01 → R-FALL-01 → NO_MATCH` | 质量≥0.70，但无证据置信度≥0.80；未建事件 |
| 快速起身控制 | 起身0.400秒；只输出 `rapid_rise` | `R-FALL-01 / GREEN` | 快速起身单独不升级 |
| 不足15秒控制 | 摆动15.964°；连续稳定7.534秒；稳定角7.436° | `NO_MATCH → NO_MATCH` | 恢复值保留7.534秒，未触发R-FALL-04 |

人工标注时间只用于核对，没有覆盖自动计算值。原始视频、33点逐帧数据、完整HTTP交换、本地路径和授权原件都保存在 Git 外。

## 已完成验收

- `POST /api/v1/assets → observations → evidence`：三包全部成功；Observation 引用、居民、来源、模拟标记和授权 C6c 资产门禁通过。
- 重复 Evidence：返回200及 `R-SYSTEM-01`，没有重复事件。
- 姿态恢复：`current_value` 为连续稳定秒数，`baseline_value=15.0`；稳定角单独保存在 `stable_trunk_angle_deg` Observation。
- Mock闭环：连续3轮得到 GREEN→ORANGE/INTERVENING→OBSERVING→RESOLVED，语义一致。
- RuleTrace：数据库和风险日志使用相同 `trace_id`；事件详情与页面直接消费后端 RuleTrace。
- 自动化：Python 88项通过；前端36项通过；生产构建通过；Mock浏览器3轮及真实API浏览器3轮通过。
- 基线：两日/三日、混机位、危险、低质量和非GREEN排除逻辑由现有后端测试覆盖；真实状态仍为“样本不足”。

## 仍需现场补充

1. 补拍一条危险组合中至少一条算法置信度达到0.80的受保护素材。
2. 从稳定达到15秒后继续完整拍摄60秒，期间不得出现新危险，才可验收 `RESOLVED`。
3. 补拍同一C6c、同一机位、至少3个不同日期的正常起身、相对步速和正常躯干角度，才能形成 `PROVISIONAL / 初步基线`。

在这些素材到位前，不使用公开数据或人工修改数值伪造完成状态。

