# 7月31日前任务拆分与完成情况

## 7月30日结论

代码交付项已完成：人工多边形区域、当前区域、ENTER/EXIT/停留、访问区域数、停留时长、区域转换、三条行为Evidence和FastAPI提交客户端均已实现。26项包内测试通过，FastAPI真实本地联调6条Observation和3条Evidence全部返回HTTP 201。

仍需成员手动完成一项实景验收：按自己的摄像头画面调整`regions.example.json`坐标，走过两个区域并确认画面标签和事件输出。该步骤用于画面适配，不是缺失代码。

## 7月25日修复后进度

- 已完成Evidence构造、Freeze v1.0校验和三条样例自动生成；
- 已完成摄像头/本地MP4统一输入、无窗口模式和自动退出；
- 已完成画面分析与标注绘制分离；
- 已完成FFmpeg预检和不含隐私的本地测试素材生成工具；
- 尚未完成区域标定、进入/离开事件、房间转换和实时Evidence接入。

## 7月25—26日：统一输出基础

- 整理语音和行为Observation字段；
- 编写Evidence构造与校验函数；
- 校验枚举、ISO 8601时间、0—1分数、`null`和必填字段；
- 让三条样例可由代码生成，而不只存在于文档中。

验收：能生成和解析三条Freeze v1.0 Evidence，且不输出最终风险等级。

## 7月27—28日：人员与区域轨迹第一版

状态：**已完成代码与确定性坐标测试，待成员摄像头实景走动一次。**

- 让行为程序支持摄像头和本地MP4；
- 在画面中人工绘制客厅、门口、走廊等多边形区域；
- 判断人物当前区域；
- 输出进入、离开和停留事件；
- 保存`source_mode`、`simulated`、`confidence`和`data_quality`。

验收：人物走过不同区域时，程序能输出正确的区域事件；本地MP4可重复运行。

## 7月29日：活动和转换统计

状态：**已完成。** 录像使用帧号/FPS计算相对时间，不将像素距离换算为米。

- 统计访问过的区域数；
- 统计区域停留时长；
- 统计区域转换次数；
- 生成单段和模拟多日统计；
- 不把像素距离解释为真实米数。

验收：同一段回放重复运行能够得到一致的区域统计。

## 7月30日：访客与停留Evidence

状态：**已完成MOCK接口联调版本。** 不宣称真实身份识别或诈骗判断。

- 区分人员检测Observation和授权判断Evidence；
- 增加`unauthorized_visitor`；
- 增加`unusual_dwell_time`；
- 明确身份不确定时的降级路径。

验收：MOCK授权名单下能够生成可解释Evidence，且不会直接判断诈骗。

## 7月31日：后端Mock接口对齐

状态：**已提前完成。** 7月30日本地FastAPI实测9项HTTP 201，另有服务未启动失败日志样例。

- 与冷雨彤确认Observation/Evidence请求格式；
- 提交样例输入和预期输出；
- 将行为统计写入Mock接口；
- 记录成功和失败日志；
- 更新README和复现命令。

验收：模拟Evidence能够进入统一接口，失败时有明确日志和MOCK/回放降级。
