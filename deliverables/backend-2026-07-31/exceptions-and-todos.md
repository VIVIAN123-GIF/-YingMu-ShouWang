# 异常及未完成清单

| 问题 | 影响 | 原因 | 当前处理 | 后续动作 |
|---|---|---|---|---|
| 三轮实机验收不稳定，完整链路仅 1/3 成功 | 尚不能把状态、抓图和播放地址作为稳定三轮能力交付 | 最新三轮状态 3/3 成功；第2轮抓图请求超时 12381 ms，第3轮播放地址请求超时 12368 ms，仅第1轮完整成功 | 最新报告全部保留，`consistent=false`、`successful_runs=1`、总体 `INCOMPLETE`；两组前序报告已独立归档 | 检查现场网络和平台响应稳定性；如需“连续三轮稳定”验收，保留当前证据后择时重跑，不得删除超时记录 |
| HLS 临时播放地址仍未成功 | 前端不能依赖标准 HLS 地址，不能声明网页实时视频能力成功 | 加密设备的 HLS 请求返回业务码 60019“加密已开启”；本地验证码不能使 protocol=2 成功 | 脚本携带本地验证码回退 protocol=1 ezopen，第1轮成功取得地址且不保存；HLS仍按失败处理 | 若必须网页播放，需核实平台对加密设备 HLS 的支持或现场评估关闭视频加密；任何设备设置变更须由保管人执行并记录隐私影响 |
| 实时视频播放未验证 | 前端和算法尚不能依赖真实连续视频流 | 临时播放地址尚未获得，未执行 HLS/FLV 地址访问或播放器验证 | 演示继续使用授权片段、抓图或明确标注的 Mock/回放链路 | 地址接口成功后验证短期地址 HTTP/内容类型、实际播放、有效期和失效行为，并保存脱敏耗时与失败日志 |
| 服务端语音与双向对讲未实机验证 | ORANGE 干预不能声明为萤石真实语音播报 | 设备/应用是否支持后端主动播报、云通话或 SDK 对讲尚未确认 | `EZVIZ_VOICE_VERIFIED=false`；端到端闭环使用明确标注的 `mock_voice` 并记录 InterventionResult | 取得官方能力与权限确认后单独验收；在真实调用日志成功前不得改为 `true` |
| WebHook 尚无平台真实回调证据 | 不能声明真实告警推送链路已经打通 | 当前只有签名、时效、幂等、脱敏和响应契约实现及单元测试 | 端点 `POST /api/v1/webhooks/ezviz` 保持“契约/单测通过” | 配置公开 HTTPS 回调，触发一次真实告警，保存脱敏 HTTP 状态、耗时、messageId 和入库结果 |
| App 基础能力三轮验收表尚未填写 | 云台、夜视、本地录像、音频和 App 对讲等能力没有现场证据 | `deliverables/device-arrival-2026-07-28/01-App基础能力三轮验收.csv` 仍为待现场填写 | 开放平台状态与抓图结果不外推为 App 其他能力 | 由设备保管人按隐私边界完成三轮现场记录；未测试项继续标记“待核验” |

## 2026-07-30 已关闭或已改善事项

- 张薇于 2026-07-30 19:25 确认平台侧旧凭证已轮换，脱敏截图在仓库外本地保存；新一轮验收未预置 AccessToken，并以 `APP_SECRET` 模式成功调用设备接口。
- 2026-07-30 15:47 的早期单轮记录曾确认设备在线：HTTP 200、业务码 200、830 ms；最新三轮结果另按上表记录，不以单轮成功替代稳定性验收。
- 2026-07-30 15:47 的早期单轮抓图曾成功：接口 HTTP 200、业务码 200、1053 ms，短期图片地址读取 HTTP 200；报告未保存图片或 URL。
- 验收脚本已将“API 查询成功”和“设备在线可用”分开：离线状态输出 `FAILED/MOCK/DEVICE_OFFLINE` 并跳过抓图、播放地址；`0/"0"/false/offline` 与在线对应值均有单元测试。真实设备离线场景仍待复验。
- 验收脚本已支持视频加密验证码仅从本地 `.env` 读取，并在 HLS 返回 60019 时回退 ezopen；验证码、图片和播放地址均不写入报告或日志。2026-07-30 19:54 的第1轮取得 ezopen 地址，证明该地址获取路径至少成功一次。
- 后端跌倒恢复状态机已接入 FastAPI：`posture_recovered → OBSERVING → 60秒观察 → RESOLVED`，三次 Mock 虚拟时间验收语义一致；该事项不再列为“只支持风险升级”的异常。
- 最新三轮脱敏验收记录和汇总已保存在 `deliverables/backend-2026-07-31/ezviz-live-validation-run-1/2/3.json`、`ezviz-live-validation-summary.json`；配置验证码前和仅尝试 HLS 的两组记录分别归档在 `ezviz-live-validation-pre-code/`、`ezviz-live-validation-with-code-hls/`。最新总体结果仍为 `INCOMPLETE`，不会将 ezopen 地址单次成功描述为 HLS、实际视频或语音能力成功。
