# 接口样例验收结果

验收时间：2026-08-03  
验收对象：语音2条Observation + 1条Evidence；行为2条Observation + 1条Evidence。

## 验收结论

| 检查项 | 结果 |
|---|---|
| 本包生成器字段与引用校验 | 6个请求全部通过 |
| `contracts.v1.models`官方模型校验 | 6个请求全部通过 |
| 当前后端Pydantic请求模型校验 | 6个请求全部通过 |
| 首次提交当前FastAPI后端 | 6个请求均为HTTP 201、`saved: true` |
| 相同内容第二次提交 | 6个请求均为HTTP 200、`idempotent: true` |
| Observation先于Evidence入库 | 通过 |
| Evidence引用的Observation存在 | 通过 |
| `resident_id/source_mode/simulated`继承 | 通过 |

## 日志

- `validation/dry-run-log.json`：不发送HTTP，只检查读取和请求顺序；
- `validation/first-submit.json`：首次真实入库的脱敏状态摘要；
- `validation/idempotent-submit.json`：第二次相同内容提交的幂等摘要。

日志不包含最终风险等级、数据库内容、账号密钥、设备信息、音视频或绝对素材路径。

## 说明

本次验收只证明Freeze v1.0字段、引用关系和当前后端接口兼容，不代表算法准确率、正式阈值或真实风险识别效果。
