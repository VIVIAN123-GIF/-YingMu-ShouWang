# 2026-08-22 修改后视频步态基线验收报告

## 一、结论

- `修改后视频.zip` 已成功解压，18 段 D1/D2/D3 三天素材全部通过 MediaPipe Pose Landmarker Heavy 姿态识别。
- 已生成三天基线包 `artifacts/modified_video_gait_acceptance/modified_video_gait_baseline_package.json`，覆盖 18 assets / 18 observations / 18 evidences。
- 后端 FastAPI TestClient 提交并读取 `/api/v1/residents/{id}/baseline` 后，`overall_status` 首次达到 **PROVISIONAL**，`rise_duration`、`relative_gait_speed`、`stable_trunk_angle_deg` 三项样本数与天数全部满足门槛。
- 这解锁了 `2026-08-16_步态未完成任务清单.md` 中最关键的 P0 项：**建立真实个人步态基线（三天）**。

## 二、素材结构

`修改后视频/视频/` 共 18 个 `.mp4`，按天/动作/序号编排：

| 天 | 起身 (RISE) | 行走 (WALK) | 站稳 (STABLE) |
|---|---|---|---|
| D1 | `D1_RISE_01_chair_C.mp4`、`D1_RISE_02_chair_C.mp4` | `D1_WALK_01`~`03` | `D1_STABLE_01_stable_S.mp4` |
| D2 | `D2_RISE_01_chair_C.mp4`、`D2_RISE_02_chair_C.mp4` | `D2_WALK_01`~`03` | `D2_STABLE_01_stable_S.mp4` |
| D3 | `D3_RISE_01_chair_C.mp4`、`D3_RISE_02_chair_C(1).mp4` | `D3_WALK_01`~`03` | `D3_STABLE_01_stable_S.mp4` |

每段视频均附带同名 `.docx` 拍摄说明（未纳入分析）。

## 三、姿态分析结果

命令：

```powershell
.\.venv\Scripts\python.exe scripts\run_modified_video_pose_review.py
```

输出：`artifacts/modified_video_review/modified_video_analysis.json`

| 类别 | 关键结果 |
|---|---|
| 有效帧率 | 12 段 `>=0.95`，最低 `D3_WALK_03=0.693`（其余帧因人物出画进入 tracking_lost） |
| 核心关键点可见度 | 全部 `>=0.91`，中位数约 `0.96` |
| 起身候选 | 6 段 RISE 全部检测出 `rapid_rise`（0.4–0.73s）；1 段 D3_WALK 意外触发（因起步初期躯干上抬） |
| 躯干摆动候选 | D2/D3 各 1 段 WALK 触发（步幅大摆动，非危险动作，进入基线时按行走处理） |
| 恢复稳定时长 | 起身 clip 7–11s，行走 clip 12–18s，站稳 clip 22–23s |

## 四、三天基线包

命令：

```powershell
.\.venv\Scripts\python.exe scripts\build_modified_video_gait_deliverables.py
```

产物：

- `artifacts/modified_video_gait_acceptance/modified_video_gait_baseline_package.json`
- `artifacts/modified_video_gait_acceptance/modified_video_gait_baseline_profile.json`

指标覆盖：

| 指标 | 样本数 | 天数 | 中位数 | MAD |
|---|---:|---:|---:|---:|
| `relative_gait_speed` | 9 | 3 | 0.068 | 0.007 |
| `rise_duration` | 6 | 3 | 0.5665 | 0.100 |
| `stable_trunk_angle_deg` | 3 | 3 | 1.052 | 0.112 |

样本落地方式：

- WALK → `sit_to_stand_duration`? 否，行走 clip 映射为 `relative_gait_speed` 观测 + `gait_stability_baseline_sample` 证据；
- RISE → `sit_to_stand_duration` 观测 + `rise_duration_baseline_sample` 证据；
- STABLE → `stable_trunk_angle_deg` 观测 + `trunk_sway_baseline_sample` 证据。

所有证据均为 `time_scale=LONG`，`source_mode=RECORDED_REPLAY`，`simulated=true`，`severity=0.05`，`confidence/quality` 直接引用 `mean_core_visibility`（0.91–0.995）。

## 五、后端接收验收

命令：

```powershell
.\.venv311\Scripts\python.exe scripts\run_modified_baseline_acceptance.py
```

产物：`artifacts/modified_video_gait_acceptance/baseline_http_acceptance.json`

后端返回（`/api/v1/residents/resident-modified-video-001/baseline`）：

| 基线项 | 样本数 | 天数 | 状态 |
|---|---:|---:|---|
| `rise_duration` | 6 | 3 | **PROVISIONAL** |
| `relative_gait_speed` | 9 | 3 | **PROVISIONAL** |
| `stable_trunk_angle_deg` | 3 | 3 | **PROVISIONAL** |

- 18 个 asset、18 个 observation、18 个 evidence 全部 `201 Created`；
- `overall_status = PROVISIONAL`；
- `observed_days = 3`（首次达到 provisional 门槛）；
- `provenance` 指向 `device-ref-c6c-modified-video-001` + `camera-position-modified-video-001`，同机位；
- `passed: true`。

## 六、剩余口径

仍不能宣称完成的内容：

1. `STABLE` 门槛（`long_days` 天）尚未达到，只能称"临时基线（provisional）"；
2. 起身时长中位数偏低（0.57s），是因 `rapid_rise` 窗口只检测 0.4–1.5s 上抬阶段，属规则口径问题而非拍摄问题；如需展示完整"起身耗时"，后续应扩展"起身完成 = 站稳 15s 起始"的度量；
3. Webhook/Worker 真实自动链路仍未联调；
4. 低照度、遮挡、出画、多人同框仍需专门补测（本批 D3_WALK 出画部分帧已展示 tracking_lost 兜底行为，但不构成完整报告）。

## 七、可复跑命令

```powershell
# 1) 姿态分析（约 5 分钟，18 段视频）
.\.venv\Scripts\python.exe scripts\run_modified_video_pose_review.py

# 2) 生成三天基线候选包
.\.venv\Scripts\python.exe scripts\build_modified_video_gait_deliverables.py

# 3) 提交后端并读回基线状态
.\.venv311\Scripts\python.exe scripts\run_modified_baseline_acceptance.py
```
