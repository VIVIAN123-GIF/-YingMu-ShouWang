# 2026-08-22 真实设备后端算法联调结论

## 结论摘要

2026-08-22 已完成一轮真实设备告警联调。当时确认 Webhook、任务入库和算法调度能够执行；
2026-08-24 复核发现，当时采集的短视频是萤石返回的 H.265 编码不兼容提示片，不是摄像头实时
画面。因此该轮不能作为“真实设备视频进入算法”的通过证据。

```text
真实设备告警
  -> 萤石消息推送
  -> 临时 HTTPS 通道
  -> POST /api/v1/webhooks/ezviz
  -> RiskAlarm / AlarmProcessingTask
  -> HLS 临时播放地址
  -> H.265 不兼容提示片
  -> FFmpeg 私有 MP4
  -> GAIT / TRAJECTORY AdapterBatch（仅证明调度，不证明真实画面算法效果）
  -> Observation 入库（输入媒体无效）
```

本轮不能认定为完整风险闭环。TRAJECTORY 正常完成并返回 `NO_EVIDENCE`；GAIT 返回
`FAILED / FEATURE_INPUT_INVALID`，脱敏复核原因是 `no_pose_detected`。因此没有新增
Evidence、RiskEvent 或 Agent Job。

统一口径：真实设备到算法调用和 Observation 入库已跑通；算法业务验收、风险事件和
Agent 链路尚未完成。

## 环境与配置结论

| 项目 | 已验证结果 |
|---|---|
| Python | `3.11.9`，使用现有 `.venv` |
| MediaPipe | PoseLandmarker 初始化成功 |
| 姿态模型 | SHA-256 校验通过 |
| FFmpeg | `9.0`；后台进程使用可验证的绝对路径 |
| SQLite CLI | `3.53.4` |
| 设备编码 | 主、子码流均固定为标准 H.264（`videoCode=1`） |
| 直播协议 | 当前现场主路径为 `EZVIZ_LIVE_PROTOCOL=4`（HTTP-FLV） |
| 视频窗口 | 当前现场互操作配置为 `YINGMU_VIDEO_CAPTURE_SECONDS=15` |
| 媒体模式 | `YINGMU_CAPTURE_MEDIA_MODE=VIDEO` |
| 算法路径 | 后台环境必须包含仓库根目录和行为适配器 `src` 目录 |
| Webhook 签名 | Live 环境保持 `EZVIZ_WEBHOOK_ALLOW_UNSIGNED_TEST=false` |

旧配置下的 `FLV + 8 秒` 和 `HLS + 4 秒` 实际受到设备 H.265 编码影响。平台返回的是固定的
H.264 编码不兼容提示片，不能据此判断实时取流能力。2026-08-24 将主、子码流切换为标准
H.264 后，后端 `/v2/live/address/get` 的 HTTP-FLV 与 HLS 均完成 15 秒真实连续画面验证。
详细证据见[萤石 H.264 实时取流恢复验证](./2026-08-24-萤石H264实时取流恢复验证.md)。

FFmpeg 通过 winget 安装后，已经运行的 PowerShell 和后台 Worker 可能继承旧 `PATH`。
部署时应在同一启动环境验证 `YINGMU_FFMPEG_BINARY` 可解析，必要时使用绝对路径。

## 23:22 真实任务结果

| 环节 | 结果 |
|---|---|
| 萤石平台推送 | 成功 |
| Webhook | HTTP `200`，告警完成去重和入库 |
| 采集 | 第 3 次任务尝试完成，生成私有 MP4 |
| 媒体 | `video/mp4`，20,290 bytes，`LIVE_DEVICE/simulated=false` |
| 媒体技术指标 | 约 `3.2 秒 / 5 FPS / 16 帧` |
| TRAJECTORY | `NO_EVIDENCE`，生成 14 条 Observation |
| GAIT | `FAILED / FEATURE_INPUT_INVALID / no_pose_detected` |
| 最终任务 | `NO_EVIDENCE`，同时保留 `PARTIAL_ALGORITHM_FAILURE` |
| Evidence / RiskEvent / Agent Job 增量 | `0 / 0 / 0` |

本轮结束后的数据库计数：

```text
risk_alarm=12
alarm_processing_task=12
asset=22
observation=104
evidence=87
risk_event=12
agent_explanation_job=2
```

这些总量包含历史数据。归因本次任务时应使用任务 ID、Asset ID 和创建时间过滤，不能仅凭
总表计数推断本次生成了风险事件。

## 当天修复项

1. 在仓库外创建并启用私有媒体目录，完成授权期限和写入能力检查。
2. 修复 FFmpeg 已安装但后台旧 `PATH` 无法解析的问题。
3. 2026-08-24 将设备主、子码流从 H.265 切换为 H.264，现场直播主路径固定为 HTTP-FLV，
   采集窗口调整为 15 秒；HLS 作为兼容性路径完成补测。
4. 修复 Worker 缺少算法 `PYTHONPATH` 导致的 `ModuleNotFoundError`。
5. 对已成功采集但算法因启动环境失败的单条任务做带条件重排；重排后算法正常执行。
6. 验证本地 API 和公网临时通道健康状态均为 `ok`。

任务重排只适用于“私有媒体存在、失败阶段为 ALGORITHM”的已确认任务，不得用于掩盖采集
失败，不得批量修改历史任务状态。

## 未完成项

1. GAIT 必须对正常授权视频返回 `NO_EVIDENCE` 或符合合同的 `LOW_QUALITY`，不能返回
   `FEATURE_INPUT_INVALID`。
2. 算法方需要明确 `no_pose_detected` 的拍摄条件和最低输入门槛。
3. 需要用授权受控风险样本生成至少一条 Evidence，并验证 RiskEvent、RuleTrace 和 Agent Job。
4. 需要验证同一任务重试时 Observation/Evidence ID 稳定且数据库无重复写入。
5. H.264 下 HTTP-FLV/HLS 已完成单轮连续视频验证；生产部署前仍需补充真实告警任务和多轮
   稳定性测试。
6. 萤石服务端语音能力仍未实机验证。

后端与算法的一次性联调准备和验收顺序见
[2026-08-24 后端与算法一次性联调说明](./2026-08-24-后端与算法一次性联调说明.md)。

## 安全与隐私

- 临时通道地址会随进程重启变化，平台回调配置必须在每轮现场测试前复核。
- 不在文档或报告中保存临时播放 URL、Token、设备序列号、验证码或 `.env` 内容。
- 临时媒体探测只保留脱敏 JSON，诊断 MP4 自动删除。
- 真实告警媒体保存在仓库和 OneDrive 之外的授权私有目录，并执行保留期限控制。
- 曾通过聊天或临时终端传递的隧道认证值应撤销并轮换，不得继续作为长期凭证使用。
- 禁止要求测试人员真实跌倒；风险样本使用授权回放或有软垫和保护人员的受控动作。
