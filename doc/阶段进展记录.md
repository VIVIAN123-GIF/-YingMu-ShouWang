# 萤石项目阶段进展记录

更新日期：2026-07-24

## 1. 当前阶段目标

根据 [任务.md](C:\Users\Bruce\Documents\挑战杯合集\萤石\doc\任务.md)，2026-07-22 至 2026-07-24 的阶段目标是：

- 安装 MediaPipe
- 跑通官方姿态估计 demo
- 下载 Pre-VFall / URFD 数据集
- 达到“能提取视频中 33 个关键点坐标”的验收标准

## 2. 已完成内容

### 2.1 MediaPipe 环境与脚本

已完成以下脚本和资源准备：

- [scripts/download_pose_model.py](C:\Users\Bruce\Documents\挑战杯合集\萤石\scripts\download_pose_model.py)
- [scripts/run_pose_demo.py](C:\Users\Bruce\Documents\挑战杯合集\萤石\scripts\run_pose_demo.py)
- [scripts/download_datasets.py](C:\Users\Bruce\Documents\挑战杯合集\萤石\scripts\download_datasets.py)
- [scripts/verify_setup.py](C:\Users\Bruce\Documents\挑战杯合集\萤石\scripts\verify_setup.py)
- [requirements.txt](C:\Users\Bruce\Documents\挑战杯合集\萤石\requirements.txt)
- [models/pose_landmarker_heavy.task](C:\Users\Bruce\Documents\挑战杯合集\萤石\models\pose_landmarker_heavy.task)

当前本地 `.venv` 已可用，能够正常运行 MediaPipe 姿态估计流程。

### 2.2 官方姿态估计 demo

已完成本地视频和 RGB 图像序列两种输入形式的验证：

- `URFD` 的 `fall-01-cam0.mp4`
- `URFD` 解压后的 `adl-01-cam0-rgb` 图像序列

输出文件位于：

- [outputs/demo](C:\Users\Bruce\Documents\挑战杯合集\萤石\outputs\demo)

当前已生成：

- `fall-01-cam0_annotated.mp4`
- `fall-01-cam0_landmarks.csv`
- `adl-01-cam0-rgb_annotated.mp4`
- `adl-01-cam0-rgb_landmarks.csv`

关键点 CSV 字段已固定为：

`source_video, frame_idx, timestamp_ms, landmark_id, x, y, z, world_x, world_y, world_z, visibility, presence`

### 2.3 33 个关键点验收

已验证 demo 输出满足“33 个关键点坐标”的目标：

- `fall-01-cam0_landmarks.csv`：可提取 33 个唯一 `landmark_id`
- `adl-01-cam0-rgb_landmarks.csv`：可提取 33 个唯一 `landmark_id`

因此，“安装 MediaPipe + 跑通官方 demo + 提取 33 点”这一主线任务已完成。

### 2.4 URFD 数据集

URFD 已按 RGB-first 方案完成下载和整理：

- `30` 条 fall 序列的 RGB 数据
- `40` 条 ADL 序列的 RGB 数据
- 对应 `70` 份 metadata CSV
- 解压后的 RGB 图像序列目录

数据与清单位于：

- [data/raw/urfd](C:\Users\Bruce\Documents\挑战杯合集\萤石\data\raw\urfd)
- [data/raw/manifest.csv](C:\Users\Bruce\Documents\挑战杯合集\萤石\data\raw\manifest.csv)

## 3. 暂未完成内容

### 3.1 Pre-VFall 数据集

Pre-VFall 当前未完成下载，原因如下：

- 官方压缩包体积约 `21 GB`
- 下载中途磁盘空间不足
- 半成品压缩包已经手动删除，避免继续占用空间

当前结论：

- Pre-VFall 不是本阶段“跑通 33 点提取”的硬阻塞项
- 当前可以先仅基于 URFD 继续推进步态特征提取
- 后续如有更大容量磁盘，再补下 Pre-VFall

## 4. 本阶段遇到的问题

### 4.1 Python 版本与 MediaPipe 兼容性

本机默认 `python` 不是稳定可用的 Python 3.11 环境，过程中出现过：

- Windows Store Python 路径失效
- `.venv` 指向失效解释器
- `py -3.11` 启动器不可稳定复用

处理结果：

- 最终重新创建了一个可工作的 `.venv`
- 当前 `.venv` 实际使用的是 `Python 3.9.25`
- 该版本已经验证可正常运行 `mediapipe 0.10.21`

### 4.2 大文件下载问题

Pre-VFall 下载时遇到：

- 磁盘空间不足
- 下载中断
- 半包文件误解压风险

处理结果：

- 已在下载脚本中加入未完成下载的校验逻辑，避免把不完整 zip 当成完整数据集处理
- 当前建议暂停 Pre-VFall，优先推进 URFD 主线任务

### 4.3 conda / venv 环境重建问题

在重建环境过程中还遇到过：

- `conda create --offline` 缺少部分缓存包
- 在线 `conda create` 访问 `repo.anaconda.com` 不稳定
- 不同目录下出现过多个项目副本，导致 `.venv` 混淆

处理结果：

- 已确认当前工作目录为 [萤石](C:\Users\Bruce\Documents\挑战杯合集\萤石)
- 已在该目录下重建可运行的 `.venv`

## 5. 当前可直接使用的命令

激活环境：

```powershell
.\.venv\Scripts\Activate.ps1
```

验证环境：

```powershell
.\.venv\Scripts\python.exe .\scripts\verify_setup.py
```

运行姿态估计 demo：

```powershell
.\.venv\Scripts\python.exe .\scripts\run_pose_demo.py --input .\data\raw\urfd\samples\fall-01-cam0.mp4 --max-frames 60
```

## 6. 下一步建议

下一阶段建议直接进入 `2026-07-25 至 2026-07-31` 的任务：

1. 基于 URFD 批量提取每帧 33 个关键点坐标
2. 编写步态特征提取脚本
3. 优先实现以下特征：
   步速、摇摆频率、步长差异比
4. 形成清洗后的可训练数据集

当前不建议把时间继续消耗在 Pre-VFall 下载上，除非后续确实需要补“跌倒前早期征兆”相关数据。
